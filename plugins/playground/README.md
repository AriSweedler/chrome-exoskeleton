# playground

A harmless place to try the keybinding engine: on a Google Doc it registers a
single key (`x`) and a chord (`gg`). The framework's own e2e specs for
multi-keystroke sequences drive these bindings, so this plugin ships with the
framework even though nobody needs it day to day. The popup tab turns it off.
