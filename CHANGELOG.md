# Changelog

Generated by `npm run bump` from conventional commits, newest first.

## 0.4.0

The framework/plugin split. The extension becomes a framework whose every
site-specific behavior is a plugin: a directory with a page module and/or a
popup tab, mounted at build time from this repo's `plugins/` and from a
machine's local dotfiles tier. Earlier history lives in the previous,
archived repository.
